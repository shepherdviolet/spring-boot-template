// The name of the environment file must be 'http-client.env.json', and it must be in the same directory as the '*.http' file
{
  "web-demo": {
    "host": "http://localhost:8000",
    "username": "foo"
  }
}